'use strict';
let number = [2, 3, 1, 9, 4];
number.sort((a, b) => a - b); //昇順ソート

number.sort((a, b) => b - a); //降順ソート

array.sort((a,b) => (a[0] - b[0])); //昇順ソート(多次元)

array.sort((a,b) => (b[0] - a[0])); //降順ソート(多次元)